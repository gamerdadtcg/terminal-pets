# Terminal Pets — art status

Art intentionally removed; mechanics only. Art will be reconnected separately.

On-chain `tokenURI` uses a placeholder SVG (rectangle + `PET#` + state). There is no pet drawing, art-pass stills, or trait catalog in this repo.
